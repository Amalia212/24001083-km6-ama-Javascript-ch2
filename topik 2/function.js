let noName = function () {
  console.log("No Name");
};

noName();

function calcAddition(number1, number2) {
  return number1 + number2;
}
console.log("calc ", calcAddition(1, 2));

const hello = (nama) => {
  return "Hello World " + nama;
};
